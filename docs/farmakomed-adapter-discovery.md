# FarmakoMed Adapter Discovery

Discovery date: 2026-05-25

Real repository:

```text
/Volumes/DevSSD/projects/farmakomed
```

## Existing Feature Flag Structure

Found:

```text
src/services/features/FeatureFlagService.ts
src/services/features/RemoteConfigService.ts
src/services/features/NoopRemoteConfigService.ts
src/services/features/CapabilityService.ts
src/diagnostics/diagnosticsFeatureFlags.ts
```

`FeatureFlagService.ts` already defines app-level boolean feature flags and uses Firebase Remote Config when Firebase is enabled. Before this adapter work, app defaults were stored in one `defaultFlags` object in `FeatureFlagService.ts`.

`diagnosticsFeatureFlags.ts` stores developer diagnostics flags locally on device. Those are not managed by the control center because they are runtime diagnostic preferences, not build/deploy feature defaults.

## Environment Separation

Found Android build separation in `package.json`:

```text
build:android:dev
build:android:uat
build:android:prd
build:android:apk:dev
build:android:apk:uat
build:android:apk:prd
```

Found Android flavor/version environment handling in:

```text
scripts/ci/build-android-apk.sh
scripts/ci/resolve-android-version.sh
android/app/build.gradle
```

No clean checked-in dev/prd feature flag default split existed before this phase.

## App Config and Environment Handling

No `app.config.ts` or `app.config.js` was found.

Environment files found by name only:

```text
.env
.env.example
actions-runner/.env
```

`.env` values were not printed or copied. Environment generation is handled by:

```text
scripts/generate-env.mjs
src/config/generatedEnv.ts
src/config/runtimeConfig.ts
src/config/buildFlags.ts
```

## Build and Validation Scripts

Useful existing scripts in FarmakoMed `package.json`:

```text
typecheck
typecheck:services
typecheck:app
lint
test
ci:validate
build:android:dev
build:android:prd
build:android:apk:dev
build:android:apk:prd
```

The contract uses `npm run ci:validate` for both dev and prd validation.

Deploy commands are configured as local artifact preparation commands:

```text
dev: npm run build:android:apk:dev
prd: npm run build:android:apk:prd
```

These commands do not implement cloud deployment. Production execution is still gated by the framework's production confirmation requirement.

## Managed Files

The safest managed files are:

```text
src/services/features/featureFlags.dev.ts
src/services/features/featureFlags.prd.ts
```

The resolver and existing service integration are:

```text
src/services/features/featureFlags.ts
src/services/features/FeatureFlagService.ts
```

The adapter only writes the environment-specific flag files. It does not silently modify unrelated files.

## Dirty File Review

During this integration pass, the FarmakoMed worktree already showed or produced changes outside the feature flag integration:

```text
android/app/build.gradle
src/config/generatedEnv.ts
```

`android/app/build.gradle` contains a Health Connect dependency version bump. This is unrelated to the adapter and was not reverted.

`src/config/generatedEnv.ts` contains an app version code update generated by FarmakoMed validation scripts. This generated file was not manually edited.

## Remaining TODOs

- Decide whether APK artifact preparation is enough for the framework's deploy action, or whether a future explicit release/publish command should be added.
- Do not add Play Console, cloud sync, or remote dashboard access without a separate approval.
